# The installer is invoked through install.php at the project root.
# Block direct web access to the wizard files themselves.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
